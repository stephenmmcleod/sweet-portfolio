.page-header
  = link_to 'Back to Gallery', admin_gallery_gallery_photos_path(@photo.gallery), :class => 'btn pull-right'
  %h2= "Editing #{@photo.title}"

= sweet_portfolio_form_for @photo, :url => { :action => :update }, :html => { :multipart => true } do |form|
  = render form