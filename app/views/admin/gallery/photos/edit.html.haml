.page-header
  = link_to 'Back to Gallery', admin_gallery_gallery_photos_path(params[:gallery_id]), :class => 'btn btn-default pull-right'
  %h2= "Editing #{@photo.title}"

= sweet_portfolio_form_for @photo, :url => { :action => :update }, :html => { :multipart => true } do |form|
  = render form